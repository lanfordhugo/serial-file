# 核心运行时依赖（实际使用）
pyserial==3.5
PyYAML==6.0.3

# 构建工具依赖（仅构建时需要）
colorama==0.4.6

# 开发和测试依赖
pytest==8.3.3
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-timeout==2.2.0

# 构建和打包依赖
pyinstaller==6.11.0
pyinstaller-hooks-contrib==2025.6 