[tool:pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short --cov=src
timeout = 60
timeout_method = signal
markers =
    slow: 标记慢速测试
    integration: 标记集成测试
    hardware: 标记需要硬件的测试 